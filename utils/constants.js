const currentWeatherIntents = ['info.weather.current', 'info.clima.current'];
const specificWeatherIntents = ['info.weather.specific', 'info.clima.specific'];

module.exports = {currentWeatherIntents, specificWeatherIntents}